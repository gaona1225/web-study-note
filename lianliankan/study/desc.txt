1、绘制背景（可以是张背景图）
2、初始化每个图片（60*60）以及随机图片位置
3、选中高光效果
4、是否可连接算法，判断两个图片是否在可消除范围（即没有阻挡，在边缘上...）
5、消除功能（如果可消除绘制连接线）
6、绘制临时连接线，线的两头分别指向可消除的两个图片的中心
7、计时
8、计分



配置文件
data.js -- 存放图片的src,alt,position,唯一的id，分组id（两个一组，用于消除相同图片标志），<图片大小，wrap背景这两个是个整体，一旦修改是修改整体>